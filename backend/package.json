{
    "name": "@iobroker/socket-client-backend",
    "version": "5.2.3",
    "description": "Client libraries for ioBroker using web sockets",
    "main": "build/index.js",
    "scripts": {
        "prepublish": "npm run build",
        "build": "tsc -p tsconfig.build.json",
        "lint": "eslint -c eslint.config.mjs",
        "test": "node -e \"require('node:fs').rmSync('build-test', { recursive: true, force: true })\" && tsc -p test/tsconfig.json && node --test \"build-test/test/**/*.test.js\"",
        "update-packages": "npx -y npm-check-updates --upgrade && cd backend && npx -y npm-check-updates --upgrade",
        "npm": "npm i -f"
    },
    "author": "",
    "license": "MIT",
    "repository": {
        "type": "git",
        "url": "git+https://github.com/ioBroker/socket-client.git",
        "directory": "backend"
    },
    "bugs": {
        "url": "https://github.com/ioBroker/socket-client/issues"
    },
    "files": [
        "src/",
        "build/",
        "package-lock.json",
        "LICENSE"
    ],
    "publishConfig": {
        "access": "public"
    },
    "homepage": "https://github.com/ioBroker/socket-client",
    "dependencies": {
        "@iobroker/socket-client": "^5.2.3",
        "ws": "^8.21.3"
    },
    "devDependencies": {
        "@iobroker/eslint-config": "^2.3.4",
        "@iobroker/types": "^7.2.3",
        "@types/node": "^22.20.4",
        "@types/ws": "^8.18.1",
        "typescript": "~6.0.3"
    }
}